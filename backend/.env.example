# Database configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=movie_night_planner
DB_POOL_SIZE=10

# Application
PORT=4000
NODE_ENV=development
SESSION_SECRET=change-this-to-a-long-random-string

# TMDB API — get a token at https://www.themoviedb.org/settings/api
TMDB_READ_TOKEN=your_tmdb_v4_read_access_token
TMDB_BASE_URL=https://api.themoviedb.org/3
TMDB_IMAGE_BASE=https://image.tmdb.org/t/p

# CORS — comma-separated list of allowed origins (production only)
# CORS_ORIGINS=https://yourdomain.com

# Rate limits (per 15 minute window)
# RATE_LIMIT_GENERAL=200
# RATE_LIMIT_AUTH=10
